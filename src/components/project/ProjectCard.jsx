import style from "../styles/projects/projectCard.module.scss";
import { BsPencil, BsFillTrashFill } from "react-icons/bs";

const ProjectCard = ({ id, name, budget, category, handleRemove }) => {
  return <p>{name}</p>;
};

export default ProjectCard;
